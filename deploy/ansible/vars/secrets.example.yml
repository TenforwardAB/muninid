---
# Copy to vars/secrets.yml and fill in. Encrypt with:
#   ansible-vault encrypt vars/secrets.yml
# vars/secrets.yml is gitignored.

muninid_database_url: "postgres://USER:PASSWORD@HOST:PORT/DB?sslmode=require"
muninid_valkey_url: "rediss://default:PASSWORD@HOST:PORT"

wildduck_api_url: "http://172.31.0.22:8080"
wildduck_api_token: "REPLACE_WITH_WILDDUCK_API_KEY"

# Optional ghcr pull credentials. Leave blank if the image is public.
ghcr_username: ""
ghcr_token: ""
